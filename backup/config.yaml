terminal:
  backend: local
  cwd: /data/workspace
  timeout: 180
compression:
  enabled: true
  threshold: 0.85
model:
  default: test
  provider: openai-api
agent: {}
onboarding:
  seen:
    profile_build_offered: true
    busy_input_prompt: true
